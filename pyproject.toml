[project]
name = "crypto-verification"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0.0",
    "requests>=2.31.0",
    "numpy>=1.26.0",
]

[project.scripts]
app = "backend.app:app"
